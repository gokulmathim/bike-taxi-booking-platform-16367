Environment configuration

Create a .env file in the project root with:
NEXT_PUBLIC_API_BASE=http://localhost:3001

This points the frontend to the Django API. For production, set this to your backend URL.
